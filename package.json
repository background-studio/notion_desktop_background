{
  "name": "notion-background-studio",
  "version": "0.2.1",
  "private": true,
  "description": "A reversible background manager for the Notion Windows desktop app.",
  "author": "Notion Background Studio contributors",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:tauri",
    "dev:tauri": "tauri dev",
    "dev:web": "vite --host 127.0.0.1",
    "build:web": "vite build",
    "build": "npm run package:tauri",
    "tauri": "tauri",
    "test": "npm run check:tauri",
    "check": "tsc --noEmit && npm run test",
    "check:tauri": "cargo test --manifest-path src-tauri/Cargo.toml",
    "package:win": "npm run package:tauri",
    "package:tauri": "tauri build"
  },
  "legacyElectronScripts": {
    "_comment": "Electron 版本已在 0.4.0 废弃；这些命令仅作历史记录，不再由 npm scripts 执行。",
    "dev:electron": "npm run build:main && concurrently -k \"tsc -p tsconfig.node.json -w --preserveWatchOutput\" \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build:main": "tsc -p tsconfig.node.json",
    "build:electron": "node -e \"require('fs').rmSync('dist-electron',{recursive:true,force:true})\" && tsc -p tsconfig.node.json && npm run build:web",
    "package:electron": "npm run build:electron && electron-builder --win nsis"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "lucide-react": "1.24.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "24.10.13",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.3",
    "typescript": "7.0.2",
    "vite": "8.1.5"
  }
}
